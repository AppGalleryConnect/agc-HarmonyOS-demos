/**
 * Copyright (c) Huawei Technologies Co., Ltd. 2020-2020. All rights reserved.
 */
import * as agconnect from '@agconnect/common-server';
export declare class HttpsCommunicator {
    private maxBodyLength;
    private maxContentLength;
    private timeout;
    private requestTemplate;
    private agcClient;
    private httpClient;
    private requestPort;
    private readonly POST;
    private readonly GET;
    private readonly PUT;
    private readonly DELETE;
    constructor(agcClient: agconnect.AGCClient);
    /**
     * This method is used to send http post request.
     *
     * @param requestUrl the url request is sent to
     * @param requestId the requestId
     * @param data body of the request
     * @param useBackup use backup gw url
     */
    sendPostRequest(requestUrl: string, requestId: string, data: any, useBackup?: boolean | undefined): Promise<any>;
    /**
     * This method is used to send http delete request.
     *
     * @param requestUrl the url request is sent to
     * @param requestId the requestId
     * @param data the body of the request
     * @param useBackup use backup gw url
     */
    sendDeleteRequest(requestUrl: string, requestId: string, data: any, useBackup?: boolean | undefined): Promise<any>;
    /**
     * This method is used to send http put request.
     *
     * @param requestUrl the url request is sent to
     * @param requestId the requestId
     * @param data the body of the request
     * @param useBackup use backup gw url
     */
    sendPutRequest(requestUrl: string, requestId: string, data: any, useBackup?: boolean | undefined): Promise<any>;
    private sendRequest;
    private generateAuth;
}
